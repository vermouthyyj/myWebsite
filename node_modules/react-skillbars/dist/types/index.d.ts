import { FC } from 'react';

declare const SkillBar: FC<SkillBarProps>;

declare type SkillBarProps = {
    skills: SkillBarSkill[];
    colors?: SkillBarColor;
    animationDelay?: number;
    animationDuration?: number;
    offset?: string;
    height?: number | string;
    animationThreshold?: number;
};
declare type SkillBarSkill = {
    type: string;
    level: number;
    color?: SkillBarColor;
};
declare type SkillBarColor = {
    bar?: string;
    title?: {
        [key: string]: string | undefined;
        text?: string;
        background?: string;
    };
};

export { SkillBar, SkillBarColor, SkillBarProps, SkillBarSkill, SkillBar as default };
